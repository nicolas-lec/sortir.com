{% extends 'base.html.twig' %}

{% block title %}Création d'une sortie{% endblock %}

{% block body %}

    <h1>Annulation</h1>


    {{ form_start(annulSortie) }}

    <div class="container">

    <div class="form-group" >
        {{ form_label((annulSortie.descriptionAnnul)) }}
        {{ form_widget(annulSortie.descriptionAnnul, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group">
        {{ form_widget(annulSortie.submit, {'attr': {'class': 'btn btn-primary'}}) }}
    </div>

    {{ form_end(annulSortie) }}

{% endblock %}